{
  "name": "katalog-kosciolow",
  "version": "1.0.0",
  "description": "Mikroserwis Nuxt do zbierania, strukturyzacji i indeksowania danych o kościołach (SEO)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "init-db": "node scripts/init-db.js",
    "directus:start": "PORT=8056 PUBLIC_URL=http://localhost:8056 npx directus start"
  },
  "dependencies": {
    "@directus/sdk": "^18.0.3",
    "axios": "^1.6.2",
    "directus": "^11.3.5",
    "dotenv": "^16.3.1",
    "nuxt": "^3.14.0",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest"
  }
}
